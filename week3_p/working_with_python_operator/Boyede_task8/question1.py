# print("                 Task1")
# Explaination of output
num1 = int(input("Enter first number: "))
num2 = int(input("Enter second number: "))


print(f'{num1} == {num2} : {num1 == num2}')
print(f"{num1} != {num2} : {num1 != num2}")
print(f"{num1} > {num2} : { num1 > num2}")
print(f"{num1} < {num2} : {num1 < num2}")

#   Comments : comparison operators are symbols using to compare two values. the outcome might either be true or false.
# 1. == check for the equality of the inputed number
# 2. != checkn if  both variables are not equal
# 3. > check if the variable on the left side is greayter than the left
# 4. < check if the variable on the left is lesser than the right side
  
  # CODES USAGE
  #The cases can be used to measure the performances of the marketing department of a company
  # can be used to measure the weigth of a patient in the hospital
  # can be used in a school to determine the overall best student in a given scores of students

#code 
initial_stock = int(input("Enter the initial stock: "))
current_stock = int(input("Enter the current stock: "))
print(f'{initial_stock} == {current_stock} : {initial_stock == current_stock}')
print(f'{initial_stock} != {current_stock} : {initial_stock != current_stock}')
print(f'{initial_stock} > {current_stock} : {initial_stock > current_stock}')
print(f'{initial_stock} < {current_stock} : {initial_stock < current_stock}')